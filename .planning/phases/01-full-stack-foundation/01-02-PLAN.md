# Plan 01-02: Supabase Schema & Auth Integration

**Objective:** Connect the app to Supabase and implement the backend authentication logic.

**Requirements Addressed:** AUTH-01, AUTH-02, AUTH-03

**Wave:** 2
**Depends On:** 01-01
**Autonomous:** True

**Files Modified:**
- [NEW] `.env.local`
- [NEW] `lib/supabase/server.ts`
- [NEW] `lib/supabase/client.ts`
- [NEW] `middleware.ts`

<tasks>
<task>
<objective>Configure Supabase Client & Server utilities</objective>
<read_first>
- .planning/phases/01-full-stack-foundation/01-RESEARCH.md
- .planning/phases/01-full-stack-foundation/01-CONTEXT.md
</read_first>
<action>
1. Install `@supabase/supabase-js` and `@supabase/ssr`.
2. Create `lib/supabase/server.ts` using the Supabase SSR pattern (cookies).
3. Create `lib/supabase/client.ts` for client-side interactions.
4. Create `.env.local.example` with keys.
</action>
<acceptance_criteria>
- `lib/supabase/server.ts` exists
- `lib/supabase/client.ts` exists
- `.env.local.example` exists
</acceptance_criteria>
</task>

<task>
<objective>Implement Auth Middleware for session persistence</objective>
<read_first>
- lib/supabase/server.ts
</read_first>
<action>
Create `middleware.ts` to update the Supabase session on every request. This ensures the session stays fresh and `AUTH-03` is satisfied.
</action>
<acceptance_criteria>
- `middleware.ts` exists in the root
- Middleware calls `supabase.auth.getUser()`
</acceptance_criteria>
</task>

<task>
<objective>Setup Auth Callback route for Google Login</objective>
<read_first>
- .planning/phases/01-full-stack-foundation/01-CONTEXT.md
</read_first>
<action>
Create `app/auth/callback/route.ts` to handle the code exchange after Google OAuth.
</action>
<acceptance_criteria>
- `app/auth/callback/route.ts` exists
- Route handles the `code` parameter and redirects correctly
</acceptance_criteria>
</task>
</tasks>

**Verification Criteria:**
- No environment variable errors in the console.
- Middleware successfully captures/refreshes user session if a cookie is present.
