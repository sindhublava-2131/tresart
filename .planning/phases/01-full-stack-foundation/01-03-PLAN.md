# Plan 01-03: Branded Auth UI Implementation

**Objective:** Build the premium, "no-compromise" login and signup pages.

**Requirements Addressed:** AUTH-04

**Wave:** 3
**Depends On:** 01-02
**Autonomous:** True

**Files Modified:**
- [NEW] `app/auth/login/page.tsx`
- [NEW] `app/auth/signup/page.tsx`
- [NEW] `components/auth/AuthForm.tsx`

<tasks>
<task>
<objective>Create the central AuthForm component</objective>
<read_first>
- .planning/phases/01-full-stack-foundation/01-CONTEXT.md
- components/ui/button.tsx (if exists from 01-01)
</read_first>
<action>
Build a reusable `AuthForm` component using Shadcn/UI (Form, Input, Button). Add Framer Motion transitions for switching between "Login" and "Signup". Include a prominent "Sign in with Google" button.
</action>
<acceptance_criteria>
- `components/auth/AuthForm.tsx` exists
- Uses Framer Motion for `initial`, `animate` props
- Contains Google OAuth button
</acceptance_criteria>
</task>

<task>
<objective>Implement Login & Signup pages</objective>
<read_first>
- app/globals.css
- components/auth/AuthForm.tsx
</read_first>
<action>
1. Create `app/auth/login/page.tsx` and `app/auth/signup/page.tsx`.
2. Wrap the `AuthForm` in a premium gallery-themed layout (Centered, high-quality white space, "Feel the Artistry" tagline).
3. Use Server Actions (defined in the components or separate file) for the Email/Password logic.
</action>
<acceptance_criteria>
- `/auth/login` page renders with the brand logo and tagline
- `/auth/signup` page renders correctly
- Form submission calls the Supabase `signUp` / `signInWithPassword` methods
</acceptance_criteria>
</task>
</tasks>

**Verification Criteria:**
- Manual verification: Login and Signup flows work correctly and "feel" premium.
- Animation between Login and Signup is smooth and GPU-accelerated.
- Validation: Proper error messages show if auth fails.
