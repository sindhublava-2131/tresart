# Plan 01-01: Environment & Project Scaffolding

**Objective:** Initialize the Next.js 15 project with Tailwind CSS, Shadcn/UI, and the luxury brand foundations.

**Requirements Addressed:** UIUX-03, UIUX-04

**Wave:** 1
**Depends On:** Nothing
**Autonomous:** True

**Files Modified:**
- [NEW] `package.json`
- [NEW] `tailwind.config.ts`
- [NEW] `app/layout.tsx`
- [NEW] `app/page.tsx`
- [NEW] `components/ui/*`

<tasks>
<task>
<objective>Scaffold Next.js 15 App Router project</objective>
<read_first>
- .planning/PROJECT.md
- .planning/phases/01-full-stack-foundation/01-CONTEXT.md
</read_first>
<action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"`
Overwrite any existing placeholder files (index.html, style.css).
</action>
<acceptance_criteria>
- `package.json` exists with next 15+
- `app/layout.tsx` exists
- `tailwind.config.ts` exists
</acceptance_criteria>
</task>

<task>
<objective>Initialize Shadcn/UI and brand theme</objective>
<read_first>
- tailwind.config.ts
- .planning/phases/01-full-stack-foundation/01-CONTEXT.md
</read_first>
<action>
1. Run `npx shadcn-ui@latest init` (choose Slate theme, use CSS variables).
2. Update `app/globals.css` with the TresArt color palette (Black/White/Soft Accents).
3. Install `framer-motion` and `lucide-react`.
</action>
<acceptance_criteria>
- `components/ui` directory exists
- `app/globals.css` contains TresArt color variables
- `package.json` contains framer-motion
</acceptance_criteria>
</task>

<task>
<objective>Clean up and set branded Home page</objective>
<read_first>
- app/page.tsx
- .planning/PROJECT.md
</read_first>
<action>
Replace `app/page.tsx` with a branded landing page using the "Feel the Artistry" tagline and the luxury minimalist style. Include a "Login" button that links to `/auth/login`.
</action>
<acceptance_criteria>
- `app/page.tsx` contains "TresArt" and "Feel the Artistry"
- No default Next.js boilerplate images/text remain
</acceptance_criteria>
</task>
</tasks>

**Verification Criteria:**
- `npm run build` passes.
- Home page renders with the correct luxury aesthetic and typography.
